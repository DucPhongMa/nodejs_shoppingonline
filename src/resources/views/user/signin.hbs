<div class="row">
    <div class="col-md-4 col-md-offset-4" style="box-shadow: 0 2px 5px 0 rgb(51 62 73 / 10%); width: 300px; margin: 68px auto;">
        <h2 class="text-center" style="font-weight: 400; letter-spacing: 1px;margin: 8px 0 34px;">Sign in</h2>
        <p style="text-align: center; color:#636d77; line-height: 1.6;">F8 là cộng đồng học lập trình thực tế miễn phí. 
        Đăng nhập để cùng nhau học tập, đóng góp và chia sẻ kiến thức ❤️
        </p>
         {{#if hasErrors}}
            <div class="alert alert-danger">
                {{#each messages}}
                    <p>{{this}}</p>
                {{/each}}
            </div>
             {{/if}}
        <form method="POST" action="/user/signin">
            <div class="form-group">
                <label for="email" style="font-weight: 700;">Email</label>
                <input type="text" name="email" class="form-control" id="email" placeholder="Enter your email...">
            </div>
            <div class="form-group">
                <label for="password" style="font-weight: 700;">Password</label>
                <input type="password" name="password" class="form-control" id="password"
                    placeholder="Enter your password...">
            </div>
           
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            <button type="submit" class="btn btn-success" style="width: 100%;">Sign In</button>
            

        </form>
    </div>
    <div class="col-xl-12 text-center">
        <p>
            <span style="color: #636d77;">Haven't had account ?</span>
            <br>
            <a href="/user/signup" class="btn btn-link" style="color: #1dbfaf; font-weight: 600; text-decoration: none;">Sign Up!</a>
        </p>
        
    </div>
    
</div>